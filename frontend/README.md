# Frontend da aplicação

## Rodando localmente

Para subir o Vite junto com a API que salva as aulas criadas pelo admin no arquivo
`data/custom-vulnerabilities.json`, rode:

```bash
npm install
npm run dev:full
```

Se preferir em dois terminais:

```bash
npm run dev:api
```

```bash
npm run dev
```
